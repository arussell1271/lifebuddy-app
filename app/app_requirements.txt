# app/app_requirements.txt

# Web Framework (FastAPI is fast and modern)
fastapi
# ASGI Server (Uvicorn provides the hot-reloading needed for development)
uvicorn[standard]
gunicorn

# Database/Vector Store Connectors
# Any DB interaction for the App Service MUST go through the Engine API or message-broker.
# Other App-specific dependencies...
redis 
# Required for App-to-Engine asynchronous task queuing.

# You will likely need additional libraries based on your AI toolkit, e.g.:
# For using pgvector easily: pgvector-python
# For Local LLMs: ollama (or an embedding model library like sentence-transformers)