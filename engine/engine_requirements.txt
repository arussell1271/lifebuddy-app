# --- Engine Service (The Brain) Requirements ---
# Focused on LLM, database, and background job processing.
# MUST include database and AI heavy dependencies.
fastapi
uvicorn[standard]
pydantic
psycopg2-binary # Postgres adapter (required for RLS/PoLP)
pgvector # Required for vector similarity search
langchain # Common LLM orchestration library
ollama # Client for the local LLM service
rq # Redis Queue or similar message queue worker library
gunicorn