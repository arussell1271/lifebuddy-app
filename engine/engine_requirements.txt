# Engine Service (The Brain) Requirements
# Focused on LLM, database, and background job processing.
# Keep this list minimal for image build stability; add heavy deps in CI if needed.
fastapi==0.95.2
httpx==0.24.1
psycopg2-binary==2.9.9
python-jose==3.3.0
prometheus_client>=0.15.0
uvicorn[standard]==0.22.0
pydantic==1.10.12
# pin pgvector to a compatible, available release used during builds
pgvector==0.4.2
jinja2
langchain
rq
gunicorn
python-jose[cryptography]
python-multipart
